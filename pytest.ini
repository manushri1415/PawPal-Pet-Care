[pytest]
# The tests import the app packages (api, pawpal_ai, pawpal_system) from the
# repository root. `python -m pytest` puts the working directory on sys.path by
# itself; plain `pytest` -- what CI runs -- does not, so say it here.
pythonpath = .
